# コーディング規約 / スタイル

- TypeScript + ESM (`"type": "module"`)。
- React は関数コンポーネント、`useState` 等のフック使用。
- ESLint (flat config) で `@eslint/js`, `typescript-eslint`, `react-hooks`, `react-refresh` を適用。
- ファイル拡張子: `*.ts` / `*.tsx` / `*.css`。
- 入口ファイルは `src/index.ts` または `src/main.tsx`。
